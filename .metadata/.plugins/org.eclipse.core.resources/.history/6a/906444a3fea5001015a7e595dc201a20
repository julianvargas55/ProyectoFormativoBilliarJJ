/* ==========================================================
   TIPOGRAFÍA Y FUENTE EXTERNA
   ========================================================== */
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap');

/* ==========================================================
   1. ESTILOS GENERALES Y LAYOUT BASE
   ========================================================== */

/* Variables de Color Finales */
:root {
    --color-fondo-general: #121212;   
    --color-panel-oscuro: #1e2832;    
    --color-panel-claro: #26333e;     
    --color-input-fondo: #31404d;     
    --color-acento-principal: #32c4a9; /* Verde-Azul (Moderno) */
    --color-acento-secundario: #ffc72c; /* Amarillo/Dorado (Títulos/Alertas) */
    --color-texto-claro: #f0f0f0;
    --color-texto-gris: #a0a0a0;
    --border-radius-base: 8px;
}

body {
    font-family: 'Poppins', sans-serif;
    margin: 0;
    padding: 0;
    background-color: var(--color-fondo-general);
    color: var(--color-texto-claro);
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
}

/* ----------------------------------------------------------
   2. ESTILOS DE LOGIN Y REGISTRO (VISTAS DE PANELES)
   ---------------------------------------------------------- */

.main-container {
    width: 90%;
    max-width: 1200px;
    height: 80vh; 
    background-color: var(--color-panel-claro);
    border-radius: 15px;
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.7);
    display: flex;
    flex-direction: column; 
    overflow: hidden; 
}

/* Barra Superior */
.top-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 40px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
}

.logo {
    font-weight: 700; 
    font-size: 1.6em;
}

.login-button, .register-button {
    background-color: transparent;
    border: 1px solid var(--color-texto-gris);
    color: var(--color-texto-gris);
    padding: 8px 18px;
    border-radius: var(--border-radius-base);
    cursor: pointer;
    transition: all 0.3s ease;
}

.login-button:hover, .register-button:hover {
    background-color: var(--color-acento-principal);
    border-color: var(--color-acento-principal);
    color: var(--color-panel-oscuro);
}

/* Paneles de Contenido */
.content-wrapper {
    flex-grow: 1; 
    display: flex;
}

.left-panel {
    flex: 0 0 350px; 
    background-color: var(--color-panel-oscuro);
    padding: 40px;
    display: flex;
    flex-direction: column;
    justify-content: center; 
}

.right-panel {
    flex-grow: 1; 
    background: linear-gradient(to right, #26333e, #1e2832);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 40px;
}

.right-panel h1 {
    font-size: 5em; 
    font-weight: 700;
    margin-bottom: 20px;
    letter-spacing: 4px;
    text-shadow: 0 0 15px rgba(0, 0, 0, 0.8);
}

.right-panel p {
    font-size: 1.1em;
    color: #c0c0c0;
    max-width: 600px;
    line-height: 1.6;
}

/* Formulario y Controles */
.input-group {
    position: relative;
    margin-bottom: 25px;
}

.input-group i {
    position: absolute;
    left: 15px;
    top: 50%;
    transform: translateY(-50%);
    color: var(--color-texto-gris); 
    font-size: 1.1em;
    transition: color 0.3s;
}

.input-group input, .left-panel select, .left-panel textarea {
    width: 100%;
    padding: 12px 12px 12px 45px; 
    border: none;
    border-radius: var(--border-radius-base);
    background-color: var(--color-input-fondo); 
    color: var(--color-texto-claro);
    box-shadow: inset 2px 2px 5px rgba(0, 0, 0, 0.4);
    box-sizing: border-box;
    outline: none; 
    transition: box-shadow 0.3s;
}

.input-group select {
    appearance: none; /* Estilo para select */
}

.input-group input:focus, .left-panel select:focus, .left-panel textarea:focus {
    box-shadow: inset 1px 1px 2px rgba(0, 0, 0, 0.6), 0 0 0 3px var(--color-acento-principal);
}

.submit-button {
    width: 100%;
    padding: 15px;
    background-color: var(--color-acento-principal);
    color: var(--color-panel-oscuro);
    font-weight: 600;
    border: none;
    border-radius: var(--border-radius-base);
    cursor: pointer;
    margin-top: 20px;
    box-shadow: 0 5px 15px rgba(50, 196, 169, 0.4);
    transition: all 0.3s ease;
}

.submit-button:hover {
    background-color: #2ab398;
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(50, 196, 169, 0.5);
}

.login-link-bottom {
    text-align: center;
    margin-top: 20px;
    font-size: 0.9em;
    color: var(--color-texto-gris);
}

.login-link-bottom a, .forgot-password a {
    color: var(--color-acento-principal); 
    text-decoration: none;
    font-weight: 600;
    transition: color 0.3s;
}

/* ----------------------------------------------------------
   3. ESTILOS DE DASHBOARD (COMPARTIDOS: BILLAR Y TEJO)
   ---------------------------------------------------------- */

.main-dashboard-container {
    display: flex;
    width: 100vw;
    height: 100vh;
    overflow: hidden; 
    background-color: var(--color-fondo-general);
    max-width: none;
    border-radius: 0; 
    box-shadow: none;
}

/* Sidebar (Menú Lateral) */
.sidebar-menu {
    flex: 0 0 250px; 
    background-color: var(--color-panel-oscuro); 
    padding: 20px 0;
    display: flex;
    flex-direction: column;
}

.logo-sidebar {
    font-size: 1.5em;
    font-weight: 700;
    text-align: center;
    padding: 10px 0 30px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.logo-sidebar i {
    color: var(--color-acento-secundario);
    margin-right: 5px;
}

.sidebar-menu nav ul {
    list-style: none;
    padding: 0;
    margin: 20px 0;
}

.sidebar-menu nav ul li a {
    display: block;
    padding: 15px 20px;
    color: #e0e0e0;
    text-decoration: none;
    transition: background-color 0.3s, color 0.3s;
}

.sidebar-menu nav ul li.active a {
    background-color: rgba(50, 196, 169, 0.15); 
    color: var(--color-acento-principal);
    border-left: 5px solid var(--color-acento-secundario); 
}

.sidebar-menu nav ul li a:hover {
    background-color: var(--color-input-fondo);
    color: var(--color-texto-claro);
}

.sidebar-footer {
    margin-top: auto;
    padding: 20px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
}

.sidebar-footer a {
    display: block;
    color: var(--color-texto-gris);
    text-decoration: none;
    text-align: center;
    transition: color 0.3s;
}

.sidebar-footer a:hover {
    color: var(--color-acento-principal);
}

/* Contenido Principal */
.dashboard-content {
    flex-grow: 1;
    padding: 30px;
    overflow-y: auto; 
    background-color: var(--color-fondo-general);
}

.dashboard-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 30px;
    border-bottom: 2px solid rgba(255, 255, 255, 0.08);
    padding-bottom: 15px;
}

.dashboard-header h1 {
    font-size: 2em;
    color: var(--color-texto-claro);
}

.user-info span {
    margin-left: 10px;
    font-weight: 600;
    color: var(--color-acento-principal); 
}

/* Métricas (Tarjetas) */
.metrics-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 25px;
    margin-bottom: 40px;
}

.metric-card {
    background-color: var(--color-panel-oscuro); 
    padding: 20px;
    border-radius: var(--border-radius-base);
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
    text-align: center;
    transition: transform 0.3s;
}

.metric-card:hover {
    transform: translateY(-5px); 
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.6);
}

.card-icon {
    font-size: 2.8em;
    margin-bottom: 10px;
    opacity: 0.8;
}

.card-icon.green { color: #28a745; }
.card-icon.blue { color: var(--color-acento-principal); }
.card-icon.yellow { color: var(--color-acento-secundario); }
.card-icon.red { color: #dc3545; }

.metric-card h2 {
    font-size: 1em;
    color: var(--color-texto-gris);
    margin: 5px 0;
}

.metric-card .value {
    font-size: 2.2em;
    font-weight: 700;
    margin-top: 10px;
    color: var(--color-texto-claro);
}

/* Lista de Mesas/Canchas en Vivo */
.live-tables h2 {
    margin-bottom: 20px;
    color: var(--color-texto-claro);
    border-left: 4px solid var(--color-acento-principal);
    padding-left: 10px;
}

.table-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.table-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 20px;
    border-radius: var(--border-radius-base);
    font-weight: 600;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
    transition: background-color 0.3s;
}

/* Colores de estado de la mesa/cancha */
.table-item.occupied { background-color: #dc3545; } /* Rojo para ocupado */
.table-item.available { background-color: #28a745; } /* Verde para disponible */
.table-item.reserved { 
    background-color: var(--color-acento-secundario); /* Amarillo para Reservado */
    color: #333; 
}
.table-item.item-low { background-color: #dc3545; } /* Rojo para stock crítico */

.table-item.reserved .action-btn {
    color: #333;
    background-color: rgba(0, 0, 0, 0.1);
}

.table-item.item-low .action-btn {
    color: var(--color-texto-claro);
    background-color: rgba(0, 0, 0, 0.3);
}

.time-elapsed {
    font-style: italic;
    font-weight: 400;
}

.action-btn {
    cursor: pointer;
    background-color: rgba(0, 0, 0, 0.3);
    padding: 8px 12px;
    border-radius: 4px;
    font-size: 0.85em;
    transition: background-color 0.2s;
}

/* ----------------------------------------------------------
   4. ESTILOS RESPONSIVOS
   ---------------------------------------------------------- */

@media (max-width: 900px) {
    .inventory-management {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 768px) {
    .main-container, .main-dashboard-container {
        flex-direction: column;
        height: auto;
        border-radius: 10px;
    }

    .top-bar {
        flex-direction: column;
        padding: 15px 20px;
    }

    .login-button, .register-button {
        margin-top: 15px;
        width: 100%;
    }

    .content-wrapper {
        flex-direction: column;
    }

    .left-panel, .right-panel, .sidebar-menu {
        flex: none;
        width: 100%;
        padding: 30px 20px;
        border-radius: 0; 
    }
    
    .sidebar-menu {
        padding: 0;
    }
    
    .logo-sidebar {
        padding: 15px;
        border-bottom: none;
    }
    
    .sidebar-menu nav ul {
        display: flex;
        overflow-x: auto; 
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        margin: 0;
    }
    
    .sidebar-menu nav ul li {
        flex-shrink: 0;
    }

    .sidebar-footer {
        display: none;
    }

    .dashboard-header {
        flex-direction: column;
        align-items: flex-start;
    }

    .dashboard-content {
        padding: 20px;
    }

    .table-item {
        flex-direction: column;
        align-items: flex-start;
        gap: 10px;
        padding: 12px 15px;
    }
    .table-number, .time-elapsed, .action-btn {
        width: 100%;
        text-align: left;
    }
}