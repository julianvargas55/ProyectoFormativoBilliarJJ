package sena.jj.com.repository; // Asegúrate de que este paquete sea correcto

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import sena.jj.com.model.Pedido; // ESTO REQUIERE QUE CREES UN MODELO Pedido.java

@Repository // Indica que esta interfaz es un Repositorio (acceso a datos)
public interface PedidoRepository extends JpaRepository<Pedido, Long> {
    // Spring Data JPA ya incluye métodos básicos como save(), findAll(), findById(), etc.
    // No necesitas escribir ninguna lógica aquí por ahora.
}